// Responsibilities:

// Receives input from the route
// Decides what to do (e.g., get all notes, create a note, update/delete a note)
// Calls helper functions (like from fileHandler.js)
// Sends the response back to the client

// Example actions:
// Add a note object with a new ID
// Filter notes by ID
// Prepare the response (res.status().json(...))